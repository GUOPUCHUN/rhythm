# RHYTHM

A personal task and condition tracker. Installs to the iPhone home screen, works offline.
Plain static files — no framework, no build step, no backend.

**[Live demo](https://your-username.github.io/rhythm/)** · replace with your Pages URL

## Why

Most habit trackers only record whether you did the thing. This one also records how the day
felt, then puts both on the same calendar — so after a few months you can ask questions like
*does finishing the main task correlate with a better mood?* or *how much does sleeping well
change the completion rate the next day?*

## Screens

**Today** — the main task with a deadline countdown and a progress bar, today's fixed tasks,
today's extra tasks, a condition questionnaire, and a self / others daily rating.

**Review** — a month calendar. Coloured dots show which task types were completed, a red dot
marks a missed day, and the bar at the bottom of each cell shows that day's condition level.
Tap any date to backfill it. Below the calendar: monthly totals and per-task progress.

**Week** — a timetable covering the full 00:00–24:00 day, drawn on a four-hour grid so a
whole week fits on one phone screen. Completed tasks are placed automatically from the times
they were logged with. Tapping an empty slot adds an own block in grey or pink: a record of
where the time actually went, which never counts towards any task total. Tapping a day header
marks it a workday, which paints a band behind that day's 09:00–18:00 — task blocks still draw
on top of it. Overlapping entries split the column width. Past weeks are browsable.

**Plan** — create and manage tasks; archive finished main tasks.

## Task types

| Type | When it appears | Planned amount |
|---|---|---|
| Main (blue) | Several can exist; Today shows only the **nearest deadline** | total by deadline |
| Fixed (amber) | Repeats on chosen **weekdays** | per week |
| Extra (green) | Only on **specific dates** | per day |

An overdue main task stays on Today until it is archived, and the next one then moves up.

## Logging

Every task can be logged as **done** or **not done**.

- Done, unit **hours** → a duration and a start time; the end time is derived.
- Done, unit **times** → an amount and a time range.
- Done entries take an optional note for how it actually went.
- Not done requires a reason. No times are asked for, and the entry never reaches the
  timetable.
- One record per task per day; tapping again edits or removes it.
- The condition entry is editable until midnight, then locked. Past days with no entry can
  still be backfilled.

Each selected mental or physical state can carry its own optional time range, so *Lonely*
and *Tired* can be pinned to different parts of the day.

## Missed days

A past day gets a red dot when any of these is true:

- a fixed task was scheduled that weekday and was never logged
- an extra task was set for that date and was never logged
- a main task reached its deadline that day without hitting its planned amount
- any task was explicitly marked not done — this one counts on the current day too, since
  it is a deliberate entry rather than a gap

Today is never counted — the day is not over. Days before a task was created are never
counted either, so adding a task does not retroactively blame you.

## Condition scoring

Every selected option adds its value: mood -2…+2 (single choice), each mental and physical
option -1 / 0 / +1 (multi-select).

| Total | Level |
|---|---|
| >= 3 | Excellent |
| 1 to 2 | Good |
| 0 | Neutral |
| -1 to -2 | Poor |
| <= -3 | Very poor |

*Calm* is exclusive with the rest of the mental group; *Normal* with the physical group.

## Data

Everything is kept in `localStorage` under the key `rhythm:v1`; nothing leaves the device and
there is no account. Review has **Copy all data** / **Paste to restore** for backups and for
moving to a new phone. Older saves are migrated automatically on load.

## Files

```
index.html              all UI and logic
manifest.webmanifest    PWA manifest
sw.js                   offline cache — bump VERSION after editing index.html
icon-180.png            apple-touch-icon (iOS only accepts PNG)
icon-192.png / icon-512.png / icon-512-maskable.png
```

## Deploy

Any static host over HTTPS. With GitHub Pages: put these files at the repository root, then
Settings → Pages → Deploy from a branch → `main` / `root`.

On iPhone open the URL in Safari, then Share → Add to Home Screen. This matters: Safari
clears tab storage after seven days without a visit, while an installed PWA keeps its own.

## Roadmap

- IndexedDB plus a Cloudflare Worker and D1 for cross-device sync
- A weekly review generated by the Claude API from the stored data
- Web Push reminders; iOS Shortcuts writing sleep and exercise data in automatically

## License

MIT
